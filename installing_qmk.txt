https://docs.qmk.fm/newbs_getting_started
https://github.com/qmk/qmk_firmware/issues/24614

sudo apt update
sudo apt upgrade
sudo apt install python3 pipx
pipx install qmk
pipx ensurepath

# close and reopen terminal

sudo apt install gcc-arm-none-eabi gcc-avr avrdude dfu-programmer dfu-util avr-libc
qmk setup

# to view a list of all available to compile
qmk list-keyboards

# to do a test compilatiom for the corne r2g
qmk compile -kb crkbd/r2g -km default

# The board you have bought is not currently in the qmk_firmware repo
git clone https://github.com/MechboardsLTD/qmk_firmware.git mechboards_qmk_firmware
cd mechboards_qmk_firmware/
git checkout corne_max
git submodule update --init --recursive

# Now when you compile you need to specify the path
QMK_HOME=~/Projects/mechboards_qmk_firmware/ qmk compile -kb mechboards/crkbd/rp2g -km via

# You should find the uf2 in mechboards_qmk_firmware/.build
# You can copy tomf_keymap into
# mechboards_qmk_firmware/keyboards/mechboards/crkbd/rp2g/keymaps
QMK_HOME=~/Projects/mechboards_qmk_firmware/ qmk compile -kb mechboards/crkbd/rp2g -km tomf_keymap


